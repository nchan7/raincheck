{
  "name": "jwt-auth-boilerplate",
  "version": "1.0.0",
  "description": "JWT Authentication Example with React",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "heroku-postbuild": "cd client && npm install --only=dev && npm install && npm run build"
  },
  "author": "Nathan Chan",
  "license": "MIT",
  "dependencies": {
    "@mapbox/mapbox-sdk": "^0.6.0",
    "axios": "^0.19.0",
    "bcrypt": "^3.0.6",
    "dotenv": "^8.0.0",
    "express": "^4.17.3",
    "express-jwt": "^5.3.1",
    "express-rate-limit": "^5.0.0",
    "helmet": "^3.19.0",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^5.6.5",
    "react-rain-animation": "^1.0.4"
  }
}
